<!DOCTYPE html>

<head>
    <title>Теодора Орачева_21180047_5.12.2023</title>
    <style>
        body {
            background-color: #FFA500;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .fn_21180047_login-container {
            background-color: #FFFFFF;
            width: 18rem;
            height: 20rem;
            padding: 20px;
            text-align: center;
        }

        h1 {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: -10px;
        }

        #email, #password {
            width: calc(100% - 22px);
            padding: 10px;
            margin: 10px;
            box-sizing: border-box;
            border: 1px solid #DDD;
            background-color: #EEE; 
        }

        #login-button {
            background-color: #87CEEB;
            color: #FFFFFF;
            padding: 9px;
            margin: 10px;
            border: none;
            cursor: pointer;
            font-size: 16px;
            box-sizing: border-box;
            width: calc(100% - 22px);
        }

        #signup-link {
            color: #800080;
            text-decoration: underline;
            display: block;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="fn_21180047_login-container">
        <h1>Login Form</h1>
        <p>Login to access your dashboard</p>

        <form>
            <input type="email" id="email" name="email" required placeholder="email@example.com">

            <input type="password" id="password" name="password" required placeholder="password">

            <button type="submit" id="login-button">Login
            </button>
        </form>

        <p>Don't have an account? <span style="text-decoration: underline; color: #800080;">Sign up</span></p>
    </div>
</body>
</html>
